{%  extends 'base.html.twig'    %}

{% block title %} Loans0  {% endblock %}
{% block body %}
<!--
array_push($data,['Creditor' => [ 'id' => ($userCreditor) ? $userCreditor->getId() :'empty' , 'name' => ($userCreditor) ? $userCreditor->getname(): 'empty'  ]
 ,'Debtor' => [ 'id' =>  ($userDebtor) ? $userDebtor->getId() : 'empty' , 'name' => ($userDebtor) ? $userDebtor->getname(): 'empty'  ]
  , 'First_Guarantor' => [ 'id' =>  ($userFirst_Guarantor) ? $userFirst_Guarantor->getId() : 'empty' , 'name' => ($userFirst_Guarantor) ? $userFirst_Guarantor->getname(): 'empty'  ]
  , 'Second_Guarantor' =>[ 'id' =>  ($userSecond_Guarantor) ? $userSecond_Guarantor->getId() : 'empty' , 'name' => ($userSecond_Guarantor) ? $userSecond_Guarantor->getname(): 'empty'  ]
'money' => $Loans->getMoney(),
'startDate' =>  $Loans->getStartDate(),
'endDate' =>  $Loans->getEndDate()
     ] )

-->
<section>
  <h1 class="text-white">s </h1>
</section>
<div  class="container mt-100" >
         <div class="row row-cols-1 row-cols-md-1">


<table class="table " >
  <!-- <caption>List of users</caption>

 -->
  <thead>
    <tr>
<th scope="col"> edit  </th>
  <th scope="col"  class="text-center">  state  </th>
      <th scope="col">  Creditor(s)  </th>
      <th scope="col">Debtor</th>
      <th scope="col">First_Guarantor</th>
      <th scope="col">Second_Guarantor</th>
      <th scope="col">Money</th>
      <th scope="col">startDate</th>
      <th scope="col">endDate</th>
    </tr>
  </thead>

  <tbody>


  {% for loan in data %}

    <tr>

              <td  class="text-center">   <a href="{{path('_edit Loan',{'id':loan.id})}}"  class="font-weight-normal float-md-right" > edit Loan </a>   </td>
              <td   class="text-center">  <a href="{{path('edit state0',{'id':loan.id})}}" class="btn btn-primary">Accept</a>  <a href="{{path('edit state0to_1',{'id':loan.id})}}" class="btn btn-danger">Reject</a>   </td>

<th scope="row">
      <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Croditors
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                {% for cr in loan.Creditor %}
              <a class="dropdown-item" href="#">{{cr.name }}</a>
               {% endfor %}
             </div>
            </li>
       </th>

    <td>   {{loan.Debtor.name }}  </td>
         <td>  {{loan.First_Guarantor.name }}   </td>
         <td> {{loan.Second_Guarantor.name }}  </td>
<td>  {{loan.money }}  </td>
<td>  {{loan.startDate }} </td>
      <td> {{loan.endDate }} </td>


    </tr>
{% endfor %}


  </tbody>


</table>
</div>
</div>






{% endblock %}
